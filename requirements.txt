Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Werkzeug==3.1.3
SQLAlchemy==2.0.36
pg8000
# ortools is required locally for timetable generation but cannot be installed on Vercel.
# Install manually in your local venv: pip install ortools
